<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Home</title>
    <style>
        /* Custom CSS styles can be added here */
        .custom-card {
           
            background-color: white;
            max-width: 500px;
            margin: auto;
            margin-top: 200px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body style="background-image: url('../assets/bg.jpg');background-position: center;background-size: cover;">
    <div class="custom-card">
        <% if (success != '') { %>
            <div id="successAlert" class="alert alert-success">
                <strong>Success!</strong> <%= success %>
            </div>
        <% } %>

        <form action="/verify" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="image" class="form-label">Upload Image:</label>
                <input type="file" class="form-control" id="image" name="image" accept="image/*" required>
            </div>
            <button type="submit" class="btn btn-primary">Verify Student</button>
        </form>
    </div>

    <script>
        // Hide the success message after 5 seconds
        setTimeout(function() {
        var successAlert = document.getElementById('successAlert');
        if (successAlert) {
            successAlert.style.display = 'none';
            // Redirect to another URL
            // window.location.href = '../cnr/index.html';
        }
    }, 1500); // Adjust the time interval (in milliseconds) as needed
    </script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/js/bootstrap.bundle.min.js" integrity="sha512-ykZ1QQr0Jy/4ZkvKuqWn4iF3lqPZyij9iRv6sGqLRdTPkY69YX6+7wvVGmsdBbiIfN/8OdsI7HABjvEok6ZopQ==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    
</body>
</html>
